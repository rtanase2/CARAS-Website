<!DOCTYPE html>
<html>

<head>
    <%= content_tag("div", nil, :id => 'invisible', :data => {:event => @event, :lang => params[:lang], :sponsors => Sponsor.all})%>
    <title> <%= @event.event_name %> </title>
    <%= javascript_include_tag  params[:event].to_s.gsub!('-', '_') << '_functions', 'data-turbolinks-track' => true %>
</head>

<body>
    <div class="jumbotron" style="margin-bottom: 0; background-image: url(<%= "/" << @event.jumbotron_img %>");">
      <div>
        <h1 style="color: white; text-shadow: -1px 0 black, 0 1px black, 1px 0 black, 0 -1px black;"> <%= @event.event_name %> </h1>
        <p style="color:white; text-shadow: -1px 0 black, 0 1px black, 1px 0 black, 0 -1px black;"> <%= @event.subheader %> </p>
      </div>
    </div>
    
    <p style="font-size: 7px; text-align: right; padding-bottom: 23px; padding-right: 10px">Photo by <a style="padding-left: 0" href="<%= @event.img_license_url %>" target="_blank"><%= @event.img_takers_name %></a>. License can be found <a style="padding-left: 0" href="<%= @event.img_license_url %>" target="_blank">here</a>.</p>

    <div class="container">
        <div class="row">
          <ol class="breadcrumb">
            <li><%= @event.language == "en" ? "Events" : "Eventos" %></li>
            <li><%= @event.event_name %></li>
          </ol>

          <ul class="nav nav-tabs">
          </ul>
          <div id="tab-container"></div>
        </div>
    </div>

    <%= render file: "layouts/footer" %>

</body>

</html>
</body>

</html>