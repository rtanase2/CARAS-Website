<%= javascript_include_tag 'navbar_functions', 'data-turbolinks-track' => true %>

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <% events = Event.where(:language => params[:lang])%>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" id="home">CARAS</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="nav-option"><a id="about-us"></a></li>
        <li class="nav-option"><a id="contact-us"></a></li>
        <li class="dropdown nav-option">
          <a id="events" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% for e in events %>
             <li><a href="<%= "events/" << e.event_type << "?lang=" << params[:lang]%>"><%= e.event_name %></a></li>
            <% end %>
          </ul>
        </li>
        <li class="nav-option"><a id="volunteer"></a></li>
        <!--<li class="nav-option"><a id="gallery"></a></li>
        <li class="nav-option"><a id='meet-the-team'></a></li>-->
        <li class="nav-option"><a id='donate'></a><li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>